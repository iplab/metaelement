<xml>
<#if (eventtype?exists)>
	<#list eventtype.row as event_type>
		<row>
			<#if (event_type.event_type_id?exists)>
				<event_type_id>${event_type.event_type_id}</event_type_id>
			</#if>
			<#if (event_type.name?exists)>
				<name>${event_type.name}</name>
			</#if>
		</row>
	</#list>
</#if>	
</xml>